server:
  port: 9000
spring:
  application:
    name: hello-server

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:1110/eureka

logging:
  level: info
  file: docker/log/logfile.log