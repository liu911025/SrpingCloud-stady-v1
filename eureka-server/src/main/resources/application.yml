server:
  port: 1111
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false     # 注册中心不向自己注册
    fetch-registry: false           # 注册中心职责维护服务实例, 不需要去检索
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka
